describe("HelloComponent", () => {

  it("should say 'Hello world!'", () => {
    expect('hello world').to.be.equal('hello world');
  });

  it("should be true",()=>{
    expect('s'==='s').to.be.equal(true);
  })
});
